// feed.js — owned by plan 01-06 (report list + map/list linkage).
//
// This is a placeholder so the app shell's script tag (index.html.tmpl,
// loaded last, after app.js/map.js/modal.js) resolves rather than 404-ing.
// Plan 01-06 replaces this file wholesale with the real list renderer,
// consuming window.Pinalert's shared store and formatting helpers exactly
// as map.js does — it must not perform its own fetch.
(function () {
  'use strict';
  // No-op guard: nothing to render yet.
}());
